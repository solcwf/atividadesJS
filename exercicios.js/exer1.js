let numeros = [1, 2, 3, 4, 5];
let soma = numeros[0] + numeros [1] + numeros [2] + numeros [3] + numeros[4];

console.log(soma);

let contador = 2

while (contador <=10) {
    console.log(contador);
    contador+=2
}